metrics-server:
  replicas: 1

  args:
    - --kubelet-insecure-tls
    - --kubelet-preferred-address-types=InternalIP,Hostname

  resources:
    requests:
      cpu: 100m
      memory: 200Mi

    limits:
      cpu: 200m
      memory: 300Mi